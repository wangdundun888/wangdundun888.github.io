### 1   匿名包
```
    之前并不理解匿名包的作用,直到看到本书12页有这么一段话:
        "这个技术是为了让Go语言对包做初始化操作,但是并不适用包里的标识符"
    Go编译器不允许声明导入一个包但不适用,所以需要 _ 来做匿名作用。
    它会调用匿名包中所有代码文件里定义的init函数，以此达到某些初始化作用。 
    程序中每个代码文件里的init函数都会在main函数执行前调用。

    思考：同一个包内不可以有可导出的同名函数，那么不可导出的函数(小写字母开头)是否可以同名？就像init函数.
        不可。
```

